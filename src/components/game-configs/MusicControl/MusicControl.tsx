import Icon from '../../ui/Icon/Icon';
import { ReactComponent as MusicIcon } from '/src/assets/on-sound.svg';

const MusicControl = () => {
  return <Icon item={<MusicIcon />} />;
};

export default MusicControl;
